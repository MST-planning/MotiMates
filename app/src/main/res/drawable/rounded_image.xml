<!-- res/drawable/rounded_image.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp" android:color="@android:color/holo_green_dark" /> <!-- 테두리 색상 설정 -->

    <!-- 크기 조절 (반지름 지정) -->
    <size
        android:width="500dp" android:height="500dp" /> <!-- 원의 세로 크기 조절 -->
</shape>
